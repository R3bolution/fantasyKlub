import { View } from "react-native";
import InicioControler from "../liga/InicioControler"; // Importación por defecto

export default function Index() {
  return (
    <View style={{ flex: 1 }}>
      <InicioControler />
    </View>
  );
}